

<ul class="nav navbar-nav navbar-right">
    <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown" style="margin-right:10px;">
          Rating
          <b class="caret"></b>
      </a>
        <ul class="dropdown-menu">
            <li>
                <%= link_to("wage_upper_bound", jobs_path(:order=>"by_lower_bound")) %>
            </li>
            <li>
                <%= link_to("wage_lower_bound", jobs_path(:order=>"by_upper_bound")) %>
            </li>
            <li>
              <%= link_to("time order", jobs_path ) %>
            </li>
        </ul>
    </li>
</ul>




<table class="table table-hover">
  <thead>
    <tr>
        <td>职缺</td>
        <td>薪资下限</td>
        <td>薪资上限</td>
        <td>刊登时间</td>
    </tr>
  </thead>


    <tbody>
        <% @jobs.each do |job| %>
      <tr>
        <td>
            <%= link_to(job.title,job_path(job)) %>
        </td>
        <td><%= job.wage_lower_bound %></td>
        <td><%= job.wage_upper_bound %></td>
        <td>
            <%= job.created_at %>
        </td>
        <td>
            <%= link_to("Edit",edit_job_path(job)) %>|
            <%= link_to("Destory",job_path(job),:method =>:delete, :data=> { :confirm =>"Are you sure?" }) %>
        </td>
    </tr>
      <% end %>
    </tbody>


</table>
