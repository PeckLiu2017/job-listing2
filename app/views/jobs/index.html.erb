

<ul class="nav navbar-nav navbar-right">
    <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown" style="margin-right:10px;">
          Rating
          <b class="caret"></b>
      </a>
        <ul class="dropdown-menu">
            <li>
                <!-- <%= link_to("wage_upper_bound", jobs_path(:order=>"by_lower_bound")) %> -->
            </li>
            <li>
              <%= link_to("time order", jobs_path ) %>
            </li>
        </ul>
    </li>
</ul>




<table class="table table-boldered show-table">
    <thead>
        <tr>
            <td>
                MOMENTS TITLE
            </td>

            <td>
              REVIEWS COUNT
            </td>
            <td>
                OPERATE
            </td>
            <td>
                CREATE AT
            </td>
        </tr>
    </thead>

    <% @jobs.each do |job| %>
    <tr>

        <td>
            <%= render_job_status(job) %>

            <%= link_to(job.title, job_path(job)) %>
        </td>
        <td> <%= job.resumes.count %> </td>
        <td>
            <%= link_to("Edit", edit_admin_job_path(job)) %>
            |
            <%= link_to("Destroy", admin_job_path(job), :method => :delete, :data => { :confirm => "Are you sure?" }) %>

        </td>
        <td>
            <%= job.created_at %>
        </td>
    </tr>
    <% end %>
</table>
